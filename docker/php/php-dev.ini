; Development overrides — mounted in docker-compose.yml only, never baked into the production image.
; Allows PHP file changes to take effect immediately without restarting the container.
opcache.validate_timestamps = 1
opcache.revalidate_freq     = 0
